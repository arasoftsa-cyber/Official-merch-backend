{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:backend:ci": "node -e \"const { spawnSync } = require('child_process'); const workers = process.env.JEST_MAX_WORKERS || '50%'; const result = spawnSync('npx', ['jest', `--maxWorkers=${workers}`], { stdio: 'inherit', shell: true }); process.exit(result.status ?? 1);\"",
    "migrate:latest": "knex migrate:latest",
    "start": "nodemon app.js",
    "migrate:rollback": "knex migrate:rollback",
    "migrate:make": "knex migrate:make",
    "backup": "node scripts/backup.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "knex": "^3.1.0",
    "nodemon": "^3.1.14",
    "openid-client": "^5.7.1",
    "pg": "^8.11.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "dependency-cruiser": "^17.3.8",
    "jest": "^30.2.0",
    "madge": "^8.0.0",
    "module-alias": "^2.3.4",
    "rimraf": "^6.1.3",
    "supertest": "^7.2.2"
  },
  "_moduleAliases": {
    "@core": "src/core",
    "@modules": "src/modules",
    "@utils": "src/utils"
  }
}
